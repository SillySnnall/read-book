// 配置项

// const host = 'http://192.168.100.96:5757'
const host = 'https://u2ms4vtq.qcloud.la'

const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}
export default config
